/*
 * Site-wide links.
 *
 * The registration form is referenced from more than one place — the hero's
 * "register here !" tag and the swimming fish — so it lives here rather than
 * being written out twice. One edit when the form moves.
 */
export const REGISTER_HREF = "https://form.typeform.com/to/VbwryQz0";
